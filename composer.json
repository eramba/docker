{
    "name": "eramba/docker",
    "description": "Docker helper files for running eramba software",
    "keywords": [
        "eramba",
        "docker"
    ],
    "type": "library",
    "homepage": "https://eramba.org",
    "authors": [
        {
            "name": "Martin Horvath",
            "email": "support@eramba.org"
        }
    ],
    "require": {
    },
    "require-dev": {
    },
    "autoload": {
    },
    "autoload-dev": {
    },
    "scripts": {
        "simple-install-enterprise": "docker compose -f docker-compose.simple-install.yml -f docker-compose.simple-install.enterprise.yml up -d --always-recreate-deps --force-recreate --remove-orphans --renew-anon-volumes",
        "simple-install-community": "docker compose -f docker-compose.simple-install.yml up -d --always-recreate-deps --force-recreate --remove-orphans --renew-anon-volumes"
    },
    "repositories": {
        "satis": {
            "type": "composer",
            "url": "https://plugins.eramba.org/"
        }
    }
}

